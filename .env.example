# Copy to .env and fill in. Used by `netlify dev` and by scripts (call-generate.mjs).
# Do not commit .env.

# Base URL of the deployed site (for generated download links and for calling the API)
SITE_URL=https://album.xxx.cz

# Same value as in Netlify → Site settings → Environment variables (for scripts that call the admin API)
ADMIN_SECRET=your-long-random-secret
